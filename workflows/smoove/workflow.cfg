includeConfig '../../confs/default.params.cfg'
includeConfig '../../confs/genomeId.params.cfg'
includeConfig '../../confs/by_app/smoove.cfg'

params	{

	/** one file containing the paths to the BAM/CRAM for controls */
	params.controls = ""
	/** one file containing the paths to the BAM/CRAM for cases */
	params.cases = ""

	params.smoove_image = ""
	/* use duphold ? slow if too many samples */
	with_duphold = false

	/** curstom exclude bed */
	exclude_bed = "NO_FILE"
	}

/** END PARAMS */
