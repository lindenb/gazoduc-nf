params {
	/** Do NOT use jvarkit/findgvcfsblocks but use the whole interval. Useful when the BED is a set of small exons (WES). */
	use_whole_block = false	
	/** extra arguments for haplotype caller */
	extraHC = ""
	parallel_combine_gvcf = false
	mapq = 20
	}
