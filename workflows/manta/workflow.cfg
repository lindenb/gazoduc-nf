includeConfig("../../confs/default.params.cfg")
includeConfig("../../confs/by_app/truvari.cfg")

params {
	/** genome Identifier */
	genomeId = ""

	/** path to indexed BAM/CRAM */
	bams = "NO_FILE"

	/** restrict to that tabix-indexed bed */
	bed = "NO_FILE"

	/** try to merge manta files at the end ? */
	with_merge_manta_vcf = false
	}

/** END PARAMS */

process {



withName : "MANTA_SINGLE_SV01:COLLECT_TO_FILE_01" {
                publishDir = [
                path:"${params.publishDir}/results/manta",
                mode: "copy",
                pattern : '*.{list}',
                overwrite : true
                ]
        }

}
