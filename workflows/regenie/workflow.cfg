params {
	prefix = ""
	outdir = "results"
	help = false

	bgen = "NO_FILE"
	covarFile = "NO_FILE"
	phenoFile = "NO_FILE"
	}

conda.enabled=true

process {

	withName : "MAKE_VCF" {
                label = "process_short"
		errorStrategy = "terminate"
		}
	withName : "ANNOT_VCF" {
                label = "process_short"
		errorStrategy = "terminate"
		}

	withName : "LD_PRUNING" {
                label = "process_medium"
		errorStrategy = "terminate"
        }


	withName : "STEP1" {
                label = "process_medium"
		errorStrategy = "terminate"
        }

}

