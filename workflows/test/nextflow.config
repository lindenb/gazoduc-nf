includeConfig "../../config/nextflow.config"

process {
    errorStrategy = "terminate"

withName: "GTF_TO_BED" {
    ext.args = "--columns gene_name,gene_biotype"
    ext.awk_expr = "(\$3==\"gene\")"
    }

}