params {
	truvari {
		/** extra parameters to add to the truvari command line**/
		extra_truvari = " --keep maxqual --refdist 500 --sizemin 50 --sizemax 50000"

		/** extra parameters to add to 'bcftools view' after 'bcftools merge' and before truvari itself. Could be a place to remove large events. */
		extra_view_truvari = ""
		}
	}
