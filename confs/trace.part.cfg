trace {
    enabled = true
    file = "${params.getOrDefault("publishDir",".")}/${params.getOrDefault("prefix","")}trace.txt"
    fields = 'task_id,hash,name,status,attempt,exit,start,complete,realtime'
}
