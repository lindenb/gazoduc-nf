nextflow_process {

    name "Test Process SAMBAMBA_MARKDUP"
    script "modules/sambamba/markdup/main.nf"
    process "SAMBAMBA_MARKDUP"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/sambamba"
    tag "modules/sambamba/markdup"

    test("BASIC") {

        when {
            process {
                """
                //input[0] = [[id:"ref"],file("${projectDir}/tests/data/rotavirus_rf.fa")]
                //input[1] = [[id:"ref"],file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
                input[0] = [[id:"S1"], file("${projectDir}/tests/data/S1.bam")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
