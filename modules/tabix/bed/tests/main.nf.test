nextflow_process {

    name "Test Process TABIX_BED"
    script "modules/tabix/bed/main.nf"
    process "TABIX_BED"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/tabix"
    tag "modules/tabix/bed"
    
    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"ref"],file("${projectDir}/tests/data/rotavirus_rf.01.bed")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
