nextflow_process {

    name "Test Process SPLIT_BED_PER_CHROMOSOME"
    script "modules/bed/splitchr/main.nf"
    process "SPLIT_BED_PER_CHROMOSOME"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/bed"
    tag "modules/bed/splitchr"

    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
		input[0] = [ [id:"bed"],file("${projectDir}/tests/data/rotavirus_rf.01.bed")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }



    test("STUB") {
	tag "stub"
	options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
		input[0] = [ [id:"bed"],file("${projectDir}/tests/data/rotavirus_rf.01.bed")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }


}
