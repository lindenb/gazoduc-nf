nextflow_process {

    name "Test Process SAMTOOLS_DICT"
    script "modules/samtools/dict/main.nf"
    process "SAMTOOLS_DICT"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/samtools"
    tag "modules/samtools/dict"

    test("Should run without failures") {

        when {
            
            process {
                """
                input[0] = [[id:"ref",ucsc_name:"hg38"],file("${projectDir}/tests/data/rotavirus_rf.fa")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
