nextflow_process {

    name "Test Process PB_BAM2FQ"
    script "modules/parabricks/bam2fq/main.nf"
    process "PB_BAM2FQ"
    tag "parabricks"
    tag "gpus"
    tag "modules"
    tag "modules/parabricks"
    tag "modules/parabricks/bam2fq"
    tag "modules/parabricks/bam2fastq"

    profile "nautilus"
    config "./nextflow.config"

    test("Should run without failures") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
		 input[0] = [[id:"S1"],
			file("${projectDir}/tests/data/S1.bam"),
			file("${projectDir}/tests/data/rotavirus_rf.fa"),
			file("${projectDir}/tests/data/rotavirus_rf.fa.fai")
			]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
