nextflow_process {

    name "Test Process DISEASES_DOWNLOAD"
    script "modules/jensenlab/diseases/download/main.nf"
    process "DISEASES_DOWNLOAD"
    tag "modules"
    tag "modules/jensenlab/diseases"
    tag "modules/jensenlab/diseases/download"
    tag "gitlab_ci"


    test("HG38") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"gtf"],file("${projectDir}/tests/data/gencode.v49.hg38.annotation.gtf.gz")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

    test("STUB") {

        options "-stub"    

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"gtf"],file("${projectDir}/tests/data/gencode.v49.hg38.annotation.gtf.gz")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
