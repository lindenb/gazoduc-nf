nextflow_process {

    name "Test Process JSON_TO_XML"
    script "modules/jvarkit/json2xml/main.nf"
    process "JSON_TO_XML"
    // tag "gitlab_ci" TODO update when conda OK
    tag "modules"
    tag "modules/jvarkit"
    tag "modules/jvarkit/json2xml"

    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
	            input[0] = Channel.of("[1,2,3,4,true,false,null,{}]").collectFile(name:"jeter.json").map{f->[[id:"zorg"],f]}
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

    test("STUB") {
        tag "gitlab_ci"
        tag "stub"
        options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
	            input[0] = Channel.of("[1,2,3,4,true,false,null,{}]").collectFile(name:"jeter.json").map{f->[[id:"zorg"],f]}
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
