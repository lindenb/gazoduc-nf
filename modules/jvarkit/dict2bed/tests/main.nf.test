nextflow_process {

    name "Test Process DICT_TO_BED"
    script "modules/jvarkit/dict2bed/main.nf"
    process "DICT_TO_BED"
    // tag "gitlab_ci" TODO update when conda OK
    tag "modules"
    tag "modules/jvarkit"
    tag "modules/jvarkit/dict2bed"

    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
	            input[0] = [[id:"dict"],file("${projectDir}/tests/data/rotavirus_rf.dict")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

    test("STUB") {
        tag "gitlab_ci"
        tag "stub"
        options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
	            input[0] = [[id:"dict"],file("${projectDir}/tests/data/rotavirus_rf.dict")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
