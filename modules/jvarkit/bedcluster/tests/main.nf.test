nextflow_process {

    name "Test Process BED_CLUSTER"
    script "modules/jvarkit/bedcluster/main.nf"
    process "BED_CLUSTER"
    // tag "gitlab_ci" TODO update when conda OK
    tag "modules"
    tag "modules/jvarkit"
    tag "modules/jvarkit/bedcluster"
    tag "gitlab_ci"
    config "./nextflow.config"

    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
	            input[0] = [[id:"dict"],file("${projectDir}/tests/data/rotavirus_rf.dict")]
	            input[1] = [[id:"bed"],file("${projectDir}/tests/data/rotavirus_rf.01.bed")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

    test("STUB") {
       
        tag "stub"
        options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
	            input[0] = [[id:"dict"],file("${projectDir}/tests/data/rotavirus_rf.dict")]
	            input[1] = [[id:"bed"],file("${projectDir}/tests/data/rotavirus_rf.01.bed")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
