nextflow_process {

    name "Test Process VCF_BED"
    script "modules/jvarkit/vcfbed/main.nf"
    process "VCF_BED"
    tag "modules"
    tag "modules/jvarkit"
    tag "modules/jvarkit/vcfbed"

    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"bed"],file("${projectDir}/tests/data/rotavirus_rf.01.bed"),[]]
                input[1] = [[id:"vcf"],file("${projectDir}/tests/data/rotavirus_rf.vcf.gz")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

    test("STUB") {
        tag "gitlab_ci"
        options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"bed"],file("${projectDir}/tests/data/rotavirus_rf.01.bed"),[]]
                input[1] = [[id:"vcf"],file("${projectDir}/tests/data/rotavirus_rf.vcf.gz")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
