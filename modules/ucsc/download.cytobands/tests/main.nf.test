nextflow_process {

    name "Test Process DOWNLOAD_CYTOBAND"
    script "modules/ucsc/download.cytobands/main.nf"
    process "DOWNLOAD_CYTOBAND"
    tag "modules"
    tag "modules/ucsc"
    tag "modules/ucsc/download.cytobands"
    tag "gitlab_ci"

    test("HG19") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"hg19",ucsc_name:"hg19"],file("${projectDir}/tests/data/human_b37.dict")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }
    
    test("HG38") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"hg38",ucsc_name:"hg38"],file("${projectDir}/tests/data/hs38me_all_chr.dict")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

    test("STUB") {
        options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"xxx",ucsc_name:"xxx"],file("${projectDir}/tests/data/rotavirus_fr.dict")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
