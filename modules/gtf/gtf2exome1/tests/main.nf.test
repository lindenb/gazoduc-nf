nextflow_process {

    name "Test Process GTF_TO_EXOME"
    script "modules/gtf/gtf2exome1/main.nf"
    process "GTF_TO_EXOME"
    config "./nextflow.config"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/gtf"
    tag "modules/gtf/gtf2exome1"



    test("STUB") {
        options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"dict","ucsc_name":"hg38",],file("${projectDir}/tests/data/hs38me_all_chr.fasta.fai")]
                input[1] = [[id:"gtf","ucsc_name":"hg38", ],file("${projectDir}/tests/data/gencode.v49.hg38.annotation.gtf.gz")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }
    
    test("BASIC") {
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"dict","ucsc_name":"hg38",],file("${projectDir}/tests/data/hs38me_all_chr.fasta.fai")]
                input[1] = [[id:"gtf","ucsc_name":"hg38", ],file("${projectDir}/tests/data/gencode.v49.hg38.annotation.gtf.gz")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }
}
