nextflow_process {

    name "Test Process SAM_TO_FASTQ"
    script "modules/gatk/sam2fastq/main.nf"
    process "SAM_TO_FASTQ"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/gatk"
    tag "modules/gatk/sam2fastq"

    test("BAM_PAIRED") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
				input[0] = [ [id:"ref"],file("${projectDir}/tests/data/rotavirus_rf.fa")]
				input[1] = [ [id:"ref"],file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
				input[2] = [ [id:"ref"],file("${projectDir}/tests/data/rotavirus_rf.dict")]
  				input[3] = [ [id:"S1"],file("${projectDir}/tests/data/S1.bam"), file("${projectDir}/tests/data/S1.bam.bai") ]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

    test("CRAM_PAIRED") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
				input[0] = [ [id:"ref"],file("${projectDir}/tests/data/rotavirus_rf.fa")]
				input[1] = [ [id:"ref"],file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
				input[2] = [ [id:"ref"],file("${projectDir}/tests/data/rotavirus_rf.dict")]
  				input[3] = [ [id:"S1"],file("${projectDir}/tests/data/S1.cram"), file("${projectDir}/tests/data/S1.cram.crai") ]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }


}
