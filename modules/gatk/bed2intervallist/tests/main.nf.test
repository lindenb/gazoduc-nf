nextflow_process {

    name "Test Process BED_TO_INTERVAL_LIST"
    script "modules/gatk/bed2intervallist/main.nf"
    process "BED_TO_INTERVAL_LIST"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/gatk"
    tag "modules/gatk/bed2intervallist"



    test("BED") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
		input[0] =  [[id:"ref",ucsc_name:"hg38"],file("${projectDir}/tests/data/rotavirus_rf.dict")]
		input[1] =  [[id:"bed",ucsc_name:"hg38"],file("${projectDir}/tests/data/rotavirus_rf.01.bed")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
