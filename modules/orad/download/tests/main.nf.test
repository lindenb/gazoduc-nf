nextflow_process {

    name "Test Process DOWNLOAD_ORAD"
    script "modules/orad/download/main.nf"
    process "DOWNLOAD_ORAD"
    tag "modules"
    tag "modules/orad"
    tag "modules/orad/download"
    //config "./nextflow.config"

    test("STUB") {
	tag "stub"
        tag "gitlab_ci"
	options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [:]
                """
            }
        }

        then {
            assert process.success
           // assert snapshot(process.out).match()
        }

    }

    
    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [:]
                """
            }
        }

        then {
            assert process.success
           // assert snapshot(process.out).match()
        }

    }

}
