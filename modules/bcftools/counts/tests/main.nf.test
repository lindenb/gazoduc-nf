nextflow_process {
    name "Test Process BCFTOOLS_COUNTS"
    script "modules/bcftools/counts/main.nf"
    process "BCFTOOLS_COUNTS"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/bcftools"
    tag "modules/bcftools/counts"
    
    test("Basic") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [
                        [id:"zorg"], 
                        file("${projectDir}/tests/data/rotavirus_rf.vcf.gz")
                        ]

                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }


    test("STUB") {
	tag "stub"
	options "-stub"

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [
                        [id:"x1"], 
                        file("${projectDir}/tests/data/rotavirus_rf.vcf.gz")
                        ]

                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }


}
