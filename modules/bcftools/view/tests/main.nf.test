nextflow_process {

    name "Test Process BCFTOOLS_VIEW"
    script "modules/bcftools/view/main.nf"
    process "BCFTOOLS_VIEW"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/bcftools"
    tag "modules/bcftools/view"

    test("BASIC") {
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"x"],[]]
                input[1] = [[id:"y"],[]]
                input[2] = [[id:"test01"], file("${projectDir}/tests/data/rotavirus_rf.vcf.gz"),[]]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }
    }

    test("STUB") {
        options "-stub"
        tag "stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"x"],[]]
                input[1] = [[id:"y"],[]]
                input[2] = [[id:"test01"], file("${projectDir}/tests/data/rotavirus_rf.vcf.gz"),[]]                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }
    }

}
