nextflow_process {

    name "Test Process DOWNLOAD_GNOMAD_SV"
    script "modules/gnomad_sv/download.vcf/main.nf"
    process "DOWNLOAD_GNOMAD_SV"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/gnomad_sv"
    tag "modules/gnomad_sv/download.vcf"

    test("HG19") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
	            input[0] = [[id:"r", ucsc_name:"hg19"],file("${projectDir}/tests/data/rotavirus_rf.fa")]
                input[1] = [[id:"r"],file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
                input[2] = [[id:"r"],file("${projectDir}/tests/data/rotavirus_rf.dict")]

                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }
    }

    test("HG38") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
	            input[0] = [[id:"r", ucsc_name:"hg38"],file("${projectDir}/tests/data/rotavirus_rf.fa")]
                input[1] = [[id:"r"],file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
                input[2] = [[id:"r"],file("${projectDir}/tests/data/rotavirus_rf.dict")]

                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }
    }

test("NOT_HUMAN") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
	            input[0] = [[id:"r", ucsc_name:"hg19"],file("${projectDir}/tests/data/rotavirus_rf.fa")]
                input[1] = [[id:"r"],file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
                input[2] = [[id:"r"],file("${projectDir}/tests/data/rotavirus_rf.dict")]

                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }
    }
}
