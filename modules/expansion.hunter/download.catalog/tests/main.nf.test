nextflow_process {

    name "Test Process XHUNTER_DOWNLOAD_CATALOG"
    script "modules/expansion.hunter/download.catalog/main.nf"
    process "XHUNTER_DOWNLOAD_CATALOG"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/expansion.hunter"
    tag "modules/expansion.hunter/download.catalog"


    test("DOWNLOAD_CATALOG") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"ref",ucsc_name:"hg38"],file("${projectDir}/tests/data/rotavirus_rf.fa")]
                input[1] = [[id:"ref",ucsc_name:"hg38"],file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
                """
            }
        }

        then {
            assert process.success
            assert snapshot(process.out).match()
        }

    }

}
