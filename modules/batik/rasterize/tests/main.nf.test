
nextflow_process {

    name "Test Process BATIK_RASTERIZE"
    script "modules/batik/rasterize/main.nf"
    process "BATIK_RASTERIZE"
    tag "modules"
    tag "modules/batik"
    tag "modules/batik/rasterize"
    tag "gitlab_ci"


  test("STUB") {
        options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"batik"],file("fake.jar")]
                input[1] = [[id:"svg"],file("fake.svg")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
