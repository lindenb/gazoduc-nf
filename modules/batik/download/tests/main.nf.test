nextflow_process {

    name "Test Process BATIK_DOWNLOAD"
    script "modules/batik/download/main.nf"
    process "BATIK_DOWNLOAD"
    tag "modules"
    tag "modules/batik"
    tag "modules/batik/download"
    tag "gitlab_ci"



    test("BASIC") {
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [:]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }


  test("STUB") {
        options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [:]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
