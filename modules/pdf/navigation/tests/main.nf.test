nextflow_process {

    name "Test Process PDF_NAVIGATION"
    script "modules/pdf/navigation/main.nf"
    process "PDF_NAVIGATION"
    tag "modules"
    tag "modules/pdf"
    tag "modules/pdf/navigation"
    tag "gitlab_ci"

    test("STUB") {
        options "-stub" 
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"pdf"],[file("file1.pdf"),file("file2.pdf"),file("file3.pdf")]]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }
}
