nextflow_process {

    name "Test Process FASTQ_MERGE"
    script "modules/fastq/merge/main.nf"
    process "FASTQ_MERGE"
    tag "modules/fastq/merge"
    tag "fastq"
    tag "gitlab_ci"

    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"S1",prefix:"Rx"], [ file("${projectDir}/tests/data/S1.R1.fq.gz"), file("${projectDir}/tests/data/S1.R2.fq.gz")]]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
