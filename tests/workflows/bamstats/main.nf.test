nextflow_pipeline {

    name "Test Workflow main.nf"
    script "workflows/bamstats/main.nf"
    tag "workflows"
    tag "workflows/bamstats"
    config "./nextflow.config"

    test("BAMQC") {

        when {
            params {
                outdir = "tests-output/results"
		fasta = "REF/GRCh38.fa"
		ucsc_name = "rotavirus"
                samplesheet = "REF/GRCh38/BAMS/samplesheet.csv"
            }
        }

        then {
            assert workflow.success
        }

    }

}
