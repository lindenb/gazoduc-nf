nextflow_process {

    name "Test Process BCFTOOLS_QUERY_SAMPLES"
    script "modules/bcftools/query.samples/main.nf"
    process "BCFTOOLS_QUERY_SAMPLES"
    tag "modules"
    tag "modules/bcftools"
    tag "modules/bcftools/query.samples"

    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"test01"], file("${projectDir}/tests/data/rotavirus_rf.vcf.gz")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
