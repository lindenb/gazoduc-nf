nextflow_process {

    name "Test Process BCFTOOLS_GUESS_PLOIDY"
    script "modules/bcftools/guess_ploidy/main.nf"
    process "BCFTOOLS_GUESS_PLOIDY"
    tag "modules"
    tag "modules/bcftools"
    tag "modules/bcftools/guess_ploidy"
    
    test("Basic") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.fa")]
                input[1] = [[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
                input[2] = [
                        [id:"vcf"], 
                        file("${projectDir}/tests/data/rotavirus_rf.vcf.gz"),
                        file("${projectDir}/tests/data/rotavirus_rf.vcf.gz.tbi")
                        ]

                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
