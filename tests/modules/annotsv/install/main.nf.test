nextflow_process {

    name "Test Process ANNOTSV_INSTALL"
    script "modules/annotsv/install/main.nf"
    process "ANNOTSV_INSTALL"
    tag "modules"
    tag "modules/annotsv"
    tag "modules/annotsv/install"
    
    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [:]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
