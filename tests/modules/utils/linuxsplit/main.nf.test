nextflow_process {

    name "Test Process LINUX_SPLIT"
    script "modules/utils/linuxsplit/main.nf"
    process "LINUX_SPLIT"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/utils"
    tag "modules/utils/linuxsplit"
    config "./nextflow.config"
    
    test("PLAIN") {
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"test01"], file("${projectDir}/tests/data/rotavirus_rf.01.bed")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }
    }

     test("GZ") {
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"test01"], file("${projectDir}/tests/data/rotavirus_rf.vcf.gz")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }
    }

    test("STUB") {
        tag "stub"
        options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"test01"], file("${projectDir}/tests/data/rotavirus_rf.01.bed")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }
    }
}
