nextflow_process {

    name "Test Process FAI2BED"
    script "modules/samtools/fai2bed/main.nf"
    process "FAI2BED"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/samtools"
    tag "modules/samtools/fai2bed"

    test("BASIC") {

        when {
            
            process {
                """
                input[0] = [[id:"ref"],file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
                """
            }
        }

        then {
            assert process.success
            // assert snapshot(process.out).match()
        }

    }

}
