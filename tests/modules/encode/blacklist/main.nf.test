nextflow_process {

    name "Test Process ENCODE_BLACKLIST"
    script "modules/encode/blacklist/main.nf"
    process "ENCODE_BLACKLIST"
    tag "modules/encode/blacklist"
    tag "fastq"
    tag "gitlab_ci"

      test("HG19") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"r",ucsc_name:"hg19"],file("${projectDir}/tests/data/rotavirus_rf.dict")]

                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }
    }

    test("HG38") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"r",ucsc_name:"hg38"],file("${projectDir}/tests/data/rotavirus_rf.dict")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }
    }

    test("NOT_HUMAN") {
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"r"],file("${projectDir}/tests/data/rotavirus_rf.dict")]

                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }
    }

}
