nextflow_process {

    name "Test Process INDEXCOV_REBUILD_BAI"
    script "modules/indexcov/rebuild.bai/main.nf"
    process "INDEXCOV_REBUILD_BAI"
    tag "modules"
    tag "modules/indexov"
    tag "modules/indexov/rebuild.bai"


    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.fa")]
                input[1] = [[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
                input[2] = Channel.of(  [[id:"S1"],"${projectDir}/tests/data/S1.bam"])
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
