nextflow_process {

    name "Test Process SNPEFF_APPLY"
    script "modules/snpeff/apply/main.nf"
    process "SNPEFF_APPLY"
    tag "modules"
    tag "modules/snpeff"
    tag "modules/snpeff/apply"

    test("STUB") {
    tag "gitlab_ci"
    tag "stub"
    options "-stub"

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"db"],file("FAKE_DIR"),file("fake.db.name")]
                input[1] = [[id:"vcf"],file("${projectDir}/tests/data/rotavirus_rf.vcf.gz")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
