nextflow_process {

    name "Test Process SNPEFF_DOWNLOAD"
    script "modules/snpeff/download/main.nf"
    process "SNPEFF_DOWNLOAD"
    tag "modules"
    tag "modules/snpeff"
    tag "modules/snpeff/download"

    test("STUB") {
    tag "gitlab_ci"
    tag "stub"
    options "-stub"

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                    input[0] = [[id:"ref", ucsc_name:"hg19"],file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
                """
            }
        }

        then {
            assert process.success
            // assert snapshot(process.out).match()
        }

    }


    test("HG19") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                    input[0] = [[id:"ref", ucsc_name:"hg19"],file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
                """
            }
        }

        then {
            assert process.success
            // assert snapshot(process.out).match()
        }

    }

}
