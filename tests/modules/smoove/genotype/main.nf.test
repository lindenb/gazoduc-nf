nextflow_process {

    name "Test Process SMOOVE_GENOTYPE"
    script "modules/smoove/genotype/main.nf"
    process "SMOOVE_GENOTYPE"
    tag "modules"
    tag "modules/smoove"
    tag "modules/smoove/genotype"

    test("STUB") {
	options "-stub"
	
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] =  [[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.fa")]
                input[1] =  [[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
                input[2] =  [[id:"xbed"], file("${projectDir}/tests/data/rotavirus_rf.vcf.gz"),file("${projectDir}/tests/data/rotavirus_rf.vcf.gz.tbi")]
                input[3] =  [[id:"S1"], file("${projectDir}/tests/data/S1.bam"),  file("${projectDir}/tests/data/S1.bam.bai")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
