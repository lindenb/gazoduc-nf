nextflow_process {

    name "Test Process SMOOVE_CALL"
    script "modules/smoove/call/main.nf"
    process "SMOOVE_CALL"
    tag "modules"
    tag "modules/smoove"
    tag "modules/smoove/call"

    test("STUB") {
	options "-stub"
	
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] =  [[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.fa")]
                input[1] =  [[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
                input[2] =  [[id:"xbed"], file("${projectDir}/tests/data/rotavirus_rf.01.bed")]
                input[3] =  [[id:"S1"], file("${projectDir}/tests/data/S1.bam"),  file("${projectDir}/tests/data/S1.bam.bai")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
