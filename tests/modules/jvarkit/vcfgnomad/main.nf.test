nextflow_process {

    name "Test Process JVARKIT_VCFGNOMAD"
    script "modules/jvarkit/vcfgnomad/main.nf"
    process "JVARKIT_VCFGNOMAD"
    tag "modules"
    tag "modules/jvarkit"
    tag "modules/jvarkit/vcfgnomad"

    test("STUB") {
        tag "gitlab_ci"
        options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"fake"],file("gnomad.vcf.gz"),file("gnomad.vcf.gz.tbi")]
                input[1] = [[id:"vcf"],file("${projectDir}/tests/data/rotavirus_rf.vcf.gz")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
