nextflow_process {

    name "Test Process BED_TO_XML"
    script "modules/jvarkit/bed2xml/main.nf"
    process "BED_TO_XML"
    // tag "gitlab_ci" TODO update when conda OK
    tag "modules"
    tag "modules/jvarkit"
    tag "modules/jvarkit/bed2xml"

    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
	            input[0] = [[id:"dict"],file("${projectDir}/tests/data/rotavirus_rf.dict")]
	            input[1] = [[id:"bed"],file("${projectDir}/tests/data/rotavirus_rf.01.bed")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

    test("STUB") {
        tag "gitlab_ci"
        tag "stub"
        options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
	            input[0] = [[id:"dict"],file("${projectDir}/tests/data/rotavirus_rf.dict")]
	            input[1] = [[id:"bed"],file("${projectDir}/tests/data/rotavirus_rf.01.bed")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
