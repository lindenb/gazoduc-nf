nextflow_process {

    name "Test Process MERGE_VCFS"
    script "modules/gatk/mergevcfs/main.nf"
    process "MERGE_VCFS"
    tag "gitlab_ci"
    tag "modules"
    tag "modules/gatk"
    tag "modules/gatk/mergevcfs"

    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"output"],[
                    file("${projectDir}/tests/data/S1.RF01.g.vcf.gz"),
                    file("${projectDir}/tests/data/S1.RF01.g.vcf.gz.tbi"),
                    file("${projectDir}/tests/data/S1.RF02.g.vcf.gz"),
                    file("${projectDir}/tests/data/S1.RF02.g.vcf.gz.tbi")
                    ]]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
