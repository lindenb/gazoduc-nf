nextflow_process {

    name "Test Process PB_GENOTYPEGVCF"
    script "modules/parabricks/genotypegvcf/main.nf"
    process "PB_GENOTYPEGVCF"
    tag "parabricks"
    tag "gpus"
    tag "modules"
    tag "modules/parabricks"
    tag "modules/parabricks/genotypegvcf"

    profile "nautilus"

    test("BASIC") {
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            process {
                """
                input[0] = [[id:"ref"],file("${projectDir}/tests/data/rotavirus_rf.fa")]
                input[1] = [[id:"ref"],file("${projectDir}/tests/data/rotavirus_rf.fa.fai")]
                input[2] = [[id:"S1"],file("${projectDir}/tests/data/S1.R01.g.vcf.gz"),file("${projectDir}/tests/data/S1.R01.g.vcf.gz.tbi")]
                """
            }
        }

        then {
            assert process.success
            //assert snapshot(process.out).match()
        }

    }

}
