nextflow_workflow {

    name "Test Workflow PREPARE_ONE_REFERENCE"
    script "subworkflows/samtools/prepare.one.ref/main.nf"
    workflow "PREPARE_ONE_REFERENCE"
    tag "subworkflows"
    tag "subworkflows/samtools"
    tag "subworkflows/samtools/prepare.one.ref"
    config "./nextflow.config"
    test("BASIC") {

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            workflow {
                """
                input[0] = [id:"rf"]
                input[1] = Channel.of([[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.fa")])
                """
            }
        }

        then {
            assert workflow.success
            //assert snapshot(workflow.out).match()
        }

    }

}
