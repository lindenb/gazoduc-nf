nextflow_workflow {

    name "Test Workflow SOMALIER_BAMS"
    script "subworkflows/somalier/bams/main.nf"
    workflow "SOMALIER_BAMS"
    tag "gitlab_ci"
    tag "subworkflows"
    tag "subworkflows/somalier"
    tag "subworkflows/somalier/bams"

    test("STUB") {
        options "-stub" // rota genome is too small

        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            workflow {
                """
                input[0] = [:]
                input[1] = Channel.of([[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.fa")])
                input[2] = Channel.of([[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.fa.fai")])
                input[3] = Channel.of([[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.dict")])
                input[4] = Channel.of(
                        [[id:"S1"],file("${projectDir}/tests/data/S1.bam"),file("${projectDir}/tests/data/S1.bam.bai")],
                        [[id:"S2"],file("${projectDir}/tests/data/S2.bam"),file("${projectDir}/tests/data/S2.bam.bai")],
                        [[id:"S3"],file("${projectDir}/tests/data/S3.bam"),file("${projectDir}/tests/data/S3.bam.bai")]
                        )
                input[5] =  Channel.of([[id:"noped"], [] ])
                input[6] =  Channel.of([[id:"sites"], [] ])
                """
            }
        }

        then {
            assert workflow.success
            //assert snapshot(workflow.out).match()
        }

    }

}
