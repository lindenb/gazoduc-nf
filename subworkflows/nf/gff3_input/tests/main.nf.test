nextflow_workflow {

    name "Test Workflow GFF3_INPUT"
    script "subworkflows/nf/gff3_input/main.nf"
    workflow "GFF3_INPUT"
    tag "gitlab_ci"
    tag "subworkflows"
    tag "subworkflows/nf"
    tag "subworkflows/nf/gff3_input"

    test("FORCE_DOWNLOAD") {
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            workflow {
                """
                input[0] = [
                    arg_name:"gff3",
                    download:true,
                    require_index:true,
                    path: null
                    ]
                input[1] = Channel.of([[id:"ref", ucsc_name:"hg38"], file("${projectDir}/tests/data/rotavirus_rf.dict")])
                """
            }
        }

        then {
            assert workflow.success
            //assert snapshot(workflow.out).match()
        }

    }


    test("STUB") {
	options "-stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            workflow {
                """
                input[0] = [
                    arg_name:"gff3",
                    download:true,
                    require_index:true,
                    path: null
                    ]
                input[1] = Channel.of([[id:"ref", ucsc_name:"hg38"], file("${projectDir}/tests/data/rotavirus_rf.dict")])
                """
            }
        }

        then {
            assert workflow.success
            //assert snapshot(workflow.out).match()
        }

    }


   test("FILENAME") {
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            workflow {
                """
                input[0] = [
                    arg_name:"gff3",
                    download:true,
                    require_index:true,
                    path : "${projectDir}/tests/data/rotavirus_rf.gff3.gz"
                    ]
                input[1] = Channel.of([[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.dict")])
                """
            }
        }

        then {
            assert workflow.success
            //assert snapshot(workflow.out).match()
        }

    }

    test("##IDX##") {
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            workflow {
                """
                input[0] = [
                    arg_name:"gff3",
                    download:true,
                    require_index:true,
                    path : "${projectDir}/tests/data/rotavirus_rf.gff3.gz##idx##${projectDir}/tests/data/rotavirus_rf.gff3.gz.tbi"
                    ]
                input[1] = Channel.of([[id:"ref"], file("${projectDir}/tests/data/rotavirus_rf.dict")])
                """
            }
        }

        then {
            assert workflow.success
            //assert snapshot(workflow.out).match()
        }

    }






}
