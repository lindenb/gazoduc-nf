nextflow_workflow {

    name "Test Workflow GTF_TO_SVG"
    script "subworkflows/circular/gtf2svg/main.nf"
    workflow "GTF_TO_SVG"
    tag "gitlab_ci"
    tag "subworkflows"
    tag "subworkflows/circular"
    tag "subworkflows/circular/gtf2svg"


    test("BASIC") {
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            workflow {
                """
                input[0]= [
                    id: "circular"
                    ]
                input[1] = Channel.of([[id:"ref","ucsc_name":"hg19"],file("${projectDir}/tests/data/rotavirus_rf.dict")])
                input[2] = Channel.empty()
                """
            }
        }

        then {
            assert workflow.success
            //assert snapshot(workflow.out).match()
        }

    }

    test("STUB") {
        options "-stub"
         tag "stub"
        when {
            params {
                // define parameters here. Example:
                // outdir = "tests/results"
            }
            workflow {
                """
                input[0]= [
                    id: "circular"
                    ]
                input[1] = Channel.of([[id:"ref","ucsc_name":"hg19"],file("${projectDir}/tests/data/rotavirus_rf.dict")])
                input[2] = Channel.empty()
                """
            }
        }

        then {
            assert workflow.success
            //assert snapshot(workflow.out).match()
        }

    }

}